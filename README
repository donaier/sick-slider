sick slider!

sizeable image slider using jquery
packed up in a middleman project

next/prev buttons
autorotation
pause on hover (or any other event)

not that special but it works!



script:

count = 0
interval = 2000                                                           - configure
transition = 1000
images = $("#gallery img")                                                - see index.html.haml for proper image placement
pp = false
ticker = null

($(images[i]).addClass("image-"+i) for i in [0..(images.length-1)])

next = () ->
  if (count < (images.length-1)) 
    count += 1
  else 
    count = 0
  show (count)                                                            - next slide
  
prev = () ->
  if (count > 0)
    count -= 1
  else 
    count = images.length-1
  show (count)                                                            - prev slide
  
show = (num) ->
  images.fadeOut(transition)
  $(".image-"+num).stop().fadeIn(transition)                              - transition time in ms

startstop = () ->                                                         - handling the hover
  clearInterval(ticker) if !pp
  ticker = null
  pp = !pp
  if (!pp)
    ticker = setInterval next, interval

$("#next").click(next)
$("#prev").click(prev)                                                    - use if required

$("#gallery").hover(startstop)

show(count)
ticker = setInterval next, interval if !pp                                    - initial start